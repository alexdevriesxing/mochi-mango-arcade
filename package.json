{
  "name": "mochi-mango-arcade",
  "version": "1.0.0",
  "private": true,
  "description": "Mochi Mango Arcade static HTML5 game portal",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "serve": "npx http-server public -p 8787 -c-1",
    "build": "npm run build:seo && npm run build:polish && npm run build:protect",
    "build:seo": "node scripts/seo-build.mjs",
    "build:polish": "node scripts/apply-ui-seo-gaio-polish.mjs && node scripts/add-polish-fixes.mjs && node scripts/final-trust-polish.mjs",
    "build:protect": "node scripts/protect-embedded-bundles.mjs",
    "validate": "node scripts/validate-site.mjs && node scripts/validate-polish.mjs",
    "audit": "node scripts/sitewide-audit.mjs",
    "smoke": "node scripts/remediation-smoke.mjs"
  },
  "devDependencies": {
    "adm-zip": "^0.6.0",
    "http-server": "14.1.1",
    "wrangler": "4.30.0"
  }
}
